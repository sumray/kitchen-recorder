<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="zq.project.kitchen.recipe.RecipeDao">

    <insert id="create" parameterType="recipe">
        INSERT INTO `RECIPE`(id, name, tags, ingredients, processDesc, createTime, updateTime)
        VALUES(#{id},#{account},#{bindingType},#{telephone},
               #{email},#{status},#{createTime},#{updateTime})
    </insert>

    <select id="get" resultType="recipe">
        select * from `RECIPE`
    </select>

</mapper>